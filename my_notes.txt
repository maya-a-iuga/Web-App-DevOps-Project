# BUILD
docker build -t web-app-devops-project .

# RUN
docker run -d -p 5000:5000 web-app-devops-project

# CONFIRM
docker ps
docker ps -a

# REMOVE
docker rmi -f web-app-devops-project

# Further notes:
#
# Try this:
docker build --progress=plain --no-cache -t web-app-devops-project .


#GIT

// create a branch and switch to the branch
$ git checkout -b <branch-name>
git checkout -b feature/add-delivery-date

// create a branch only
$ git branch <branch-name>

// Having made code changes.
git add .
git commit -m "Meaningful comment"
// Push new (local) branch to remote repo:
$ git push --set-upstream origin <new-branch-name>
$ git push -u origin <new-branch-name>
git push -u origin feature/add-delivery-date

